apiVersion: v1
kind: Service
metadata:
  name: node-demo
  labels:
    tier: apps
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
      name: http-node-demo
  selector:
    app: node-demo-server
    tier: apps
  sessionAffinity: None
  type: ClusterIP
